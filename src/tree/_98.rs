use super::{TreeNode, prelude::*};

struct Solution;

impl Solution {
    pub fn is_valid_bst(root: Option<Rc<RefCell<TreeNode>>>) -> bool {
       unimplemented!() 
    }

    fn inorder_traversal(root: Option<&Rc<RefCell<TreeNode>>>) -> Result<Option<i32>,bool> {
        unimplemented!()
    }
}